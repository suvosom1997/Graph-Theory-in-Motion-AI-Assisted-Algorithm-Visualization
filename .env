OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# Note: DO NOT commit the real `.env` with secrets. Keep the actual key in a secure store or in your local
# copy of `.env` which should be excluded from version control (see .gitignore).
